---
phase: 01-core-editor-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/rollup.config.js
  - frontend/src/editor/index.ts
  - frontend/src/editor/dashboard-editor-component.ts
  - frontend/src/editor/editor-toolbar.ts
  - examples/editor-demo.html
autonomous: true

must_haves:
  truths:
    - "User can navigate to /editor-demo.html and see the editor UI"
    - "Editor displays toolbar, left sidebar (palette), canvas area, right sidebar (config)"
    - "Editor fills viewport height (100vh)"
  artifacts:
    - path: "frontend/src/editor/dashboard-editor-component.ts"
      provides: "Main editor shell component"
      exports: ["DashboardEditorComponent"]
    - path: "frontend/src/editor/editor-toolbar.ts"
      provides: "Toolbar with placeholder buttons"
      exports: ["EditorToolbar"]
    - path: "frontend/src/editor/index.ts"
      provides: "Barrel export for editor module"
    - path: "examples/editor-demo.html"
      provides: "Demo page for editor"
  key_links:
    - from: "frontend/rollup.config.js"
      to: "frontend/src/editor/index.ts"
      via: "input entry point"
      pattern: "src/editor/index.ts"
    - from: "examples/editor-demo.html"
      to: "frontend/dist/dashboard-editor-component.js"
      via: "script module import"
      pattern: "dashboard-editor-component.js"
---

<objective>
Create the dashboard editor shell with toolbar and layout structure.

Purpose: Establish the editor foundation that Phase 1 Plan 02 will extend with the grid canvas.
Output: Working editor component with flexbox layout (toolbar, sidebars, canvas area).
</objective>

<execution_context>
@/Users/fokot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fokot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-editor-framework/01-RESEARCH.md
@frontend/src/dashboard-component.ts
@frontend/rollup.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Interactjs and create editor folder structure</name>
  <files>frontend/package.json, frontend/src/editor/index.ts</files>
  <action>
    1. Run `cd frontend && pnpm add interactjs` to install Interactjs dependency
    2. Create `frontend/src/editor/` directory
    3. Create `frontend/src/editor/index.ts` as barrel export (empty for now, will export components as they're created)
    
    The barrel export should use `.js` extension for imports (Lit convention in this codebase).
  </action>
  <verify>
    - `pnpm list interactjs` shows version ^1.10.x installed
    - `frontend/src/editor/index.ts` exists
  </verify>
  <done>Interactjs installed, editor folder structure created</done>
</task>

<task type="auto">
  <name>Task 2: Create editor-toolbar component</name>
  <files>frontend/src/editor/editor-toolbar.ts</files>
  <action>
    Create `editor-toolbar.ts` as a Lit component with:
    - Element name: `editor-toolbar`
    - Height: 48px, horizontal flexbox layout
    - Contains: placeholder buttons for Save, Undo, Redo (disabled, just visual for now)
    - Left section: title "Dashboard Editor"
    - Right section: action buttons
    - Follow existing Lit patterns from `dashboard-component.ts` (decorators, css template literal)
    - Use `.js` extension for relative imports
    
    Styling: Light background (#f8f9fa), bottom border, flex with space-between.
  </action>
  <verify>TypeScript compiles without errors: `cd frontend && npx tsc --noEmit`</verify>
  <done>EditorToolbar component created with placeholder Save/Undo/Redo buttons</done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard-editor-component shell</name>
  <files>frontend/src/editor/dashboard-editor-component.ts, frontend/src/editor/index.ts</files>
  <action>
    Create `dashboard-editor-component.ts` as main editor shell:
    - Element name: `dashboard-editor-component`
    - Layout: Flexbox column for full viewport height (100vh)
    - Structure: toolbar (48px) + main area (flex: 1)
    - Main area: left sidebar (250px) + canvas area (flex: 1) + right sidebar (280px)
    - Sidebars have placeholder content: "Widget Palette" (left), "Configuration" (right)
    - Canvas area: just a div with class `.canvas-area` and placeholder text "Canvas Area"
    - Import and use `<editor-toolbar>` component
    
    Update `index.ts` to export both components.
    
    Follow Lit patterns: @customElement decorator, static styles with css template literal.
    Use `.js` extension for local imports.
  </action>
  <verify>TypeScript compiles: `cd frontend && npx tsc --noEmit`</verify>
  <done>DashboardEditorComponent shell created with toolbar and 3-column layout</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm list interactjs` shows installed
2. `cd frontend && npx tsc --noEmit` compiles without errors
3. `cd frontend && pnpm run build` succeeds
4. Editor components exist in `frontend/src/editor/`
</verification>

<success_criteria>
- Interactjs is installed as dependency
- Editor folder structure created with barrel export
- EditorToolbar component renders toolbar with placeholder buttons
- DashboardEditorComponent renders shell with toolbar + 3-column layout
- Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-editor-framework/01-01-SUMMARY.md`
</output>

