---
phase: 01-core-editor-framework
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/editor/editor-canvas.ts
  - frontend/src/editor/dashboard-editor-component.ts
  - frontend/src/editor/index.ts
  - frontend/rollup.config.js
  - examples/editor-demo.html
autonomous: true

must_haves:
  truths:
    - "User can navigate to /editor-demo.html and see 12-column grid lines on canvas"
    - "Grid lines are visually distinct indicating widget placement columns"
    - "Canvas fills available space in the editor layout"
  artifacts:
    - path: "frontend/src/editor/editor-canvas.ts"
      provides: "Grid canvas component with visible grid lines"
      exports: ["EditorCanvas"]
    - path: "examples/editor-demo.html"
      provides: "Demo page to test editor in browser"
  key_links:
    - from: "frontend/rollup.config.js"
      to: "frontend/src/editor/index.ts"
      via: "input entry point"
      pattern: "src/editor/index.ts"
    - from: "examples/editor-demo.html"
      to: "frontend/dist/dashboard-editor-component.js"
      via: "script module import"
      pattern: "dashboard-editor-component.js"
    - from: "frontend/src/editor/dashboard-editor-component.ts"
      to: "frontend/src/editor/editor-canvas.ts"
      via: "imports and renders <editor-canvas>"
      pattern: "editor-canvas"
    - from: "frontend/src/editor/editor-canvas.ts"
      to: "CSS Grid"
      via: "grid-template-columns: repeat(12, 1fr)"
      pattern: "grid-template-columns"
---

<objective>
Create the grid canvas component with visible 12-column grid lines.

Purpose: Provide visual grid structure for widget placement (LAYOUT-01 requirement).
Output: EditorCanvas component with CSS Grid layout and visible grid lines via repeating-linear-gradient.
</objective>

<execution_context>
@/Users/fokot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fokot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-editor-framework/01-RESEARCH.md
@.planning/phases/01-core-editor-framework/01-01-SUMMARY.md
@frontend/src/dashboard-component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create editor-canvas component with visible grid</name>
  <files>frontend/src/editor/editor-canvas.ts, frontend/src/editor/index.ts</files>
  <action>
    Create `editor-canvas.ts` as a Lit component:
    - Element name: `editor-canvas`
    - CSS Grid layout: `grid-template-columns: repeat(12, 1fr)` with 16px gap
    - Min-height: 600px, background: #f5f5f5
    - Visible grid lines using CSS `repeating-linear-gradient`:
      - Vertical lines at column boundaries (#e0e0e0, 1px wide)
      - Optional: horizontal lines for row guidance
    - Position: relative (for future absolute positioned drag elements)
    - Store grid config as properties: columns (default 12), gap (default 16)
    
    Research pattern from 01-RESEARCH.md:
    ```css
    background-image: 
      repeating-linear-gradient(
        to right,
        transparent,
        transparent calc(100% / 12 - 1px),
        #e0e0e0 calc(100% / 12 - 1px),
        #e0e0e0 calc(100% / 12)
      );
    ```
    
    Update `index.ts` to export EditorCanvas.
  </action>
  <verify>
    - TypeScript compiles: `cd frontend && npx tsc --noEmit`
    - EditorCanvas is exported from index.ts
  </verify>
  <done>EditorCanvas component created with 12-column CSS Grid and visible grid lines</done>
</task>

<task type="auto">
  <name>Task 2: Integrate canvas into editor shell</name>
  <files>frontend/src/editor/dashboard-editor-component.ts</files>
  <action>
    Update `dashboard-editor-component.ts`:
    1. Import `./editor-canvas.js` (triggers side-effect registration)
    2. Replace placeholder canvas div with `<editor-canvas></editor-canvas>`
    3. Ensure canvas area has overflow: auto for scrolling large canvases
    4. Canvas should fill available space (flex: 1)
  </action>
  <verify>TypeScript compiles: `cd frontend && npx tsc --noEmit`</verify>
  <done>EditorCanvas integrated into DashboardEditorComponent</done>
</task>

<task type="auto">
  <name>Task 3: Add rollup entry point and create demo page</name>
  <files>frontend/rollup.config.js, examples/editor-demo.html</files>
  <action>
    1. Add new rollup entry point for dashboard-editor-component:
       - input: 'src/editor/index.ts'
       - output: 'dist/dashboard-editor-component.js'
       - Same plugins as dashboard-component (replace, typescript, nodeResolve, terser)
    
    2. Create `examples/editor-demo.html`:
       - Similar structure to dashboard-demo.html
       - Title: "Dashboard Editor Demo"
       - Import: `<script type="module" src="/frontend/dist/dashboard-editor-component.js">`
       - Body: `<dashboard-editor-component></dashboard-editor-component>`
       - Minimal styling: body margin 0, full viewport
       - Back link to index.html
    
    3. Run build: `cd frontend && pnpm run build`
  </action>
  <verify>
    - Build succeeds: `cd frontend && pnpm run build`
    - `frontend/dist/dashboard-editor-component.js` exists
    - `examples/editor-demo.html` exists
  </verify>
  <done>Rollup configured, demo page created, build succeeds</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm run build` succeeds without errors
2. `frontend/dist/dashboard-editor-component.js` exists and is non-empty
3. Open `examples/editor-demo.html` in browser (via `cd frontend && pnpm run serve`)
4. Editor UI visible: toolbar at top, left sidebar (palette), canvas with grid lines, right sidebar (config)
5. Canvas shows visible 12-column grid line pattern
6. Editor fills viewport height
</verification>

<success_criteria>
- EditorCanvas renders 12-column CSS Grid with visible grid lines
- Grid lines are visually distinct (light gray on slightly different gray background)
- DashboardEditorComponent integrates toolbar + sidebars + canvas
- Demo page loads and displays complete editor shell
- Build completes without errors
- EDITOR-01 satisfied: User can open editor demo and see editor UI
- LAYOUT-01 partially satisfied: Canvas displays 12-column visual grid structure
  - Note: Snap-to-grid BEHAVIOR (widgets snapping when dragged) is Phase 2 "Widget Interaction" (EDITOR-02/03)
  - Phase 1 provides the visual grid structure ready to receive widgets
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-editor-framework/01-02-SUMMARY.md`
</output>

